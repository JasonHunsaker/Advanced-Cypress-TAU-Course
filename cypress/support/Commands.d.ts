/// <reference types="cypress" />

declare namespace Cypress {
    interface Chainable{

        /**Creates a new board via UI
         * just put the board name ('here')
         */
        addBoard()
    }

     
}

